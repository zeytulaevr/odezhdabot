# Telegram Shop Bot

Production-ready Telegram бот для магазина одежды с форумными топиками, системой мониторинга и алертов.

> 🚀 **Новичок?** Начните с [Быстрого старта](docs/GETTING_STARTED.md) - запустите бота за 10 минут!

## Возможности

### 🛍 Магазин
- Каталог товаров с категориями
- Привязка товаров к форумным топикам Telegram
- Система заказов с отслеживанием статусов
- Корзина и оформление заказа
- Отзывы с модерацией

### 👥 Управление пользователями
- Трехуровневая система ролей (user, admin, super_admin)
- Отслеживание активности пользователей
- Блокировка пользователей

### 📢 Рассылки
- Массовая рассылка всем пользователям или по фильтрам
- Поддержка текста, фото, видео, документов
- Inline-кнопки в рассылках
- Отслеживание статистики доставки (success rate)
- История рассылок

### 📊 Мониторинг и аналитика
- Comprehensive metrics collection (30+ метрик)
- Статистика в реальном времени (общая, за сегодня, неделю, месяц)
- Health check системных компонентов
- Structured logging с JSON форматом
- Автоматическая маскировка чувствительных данных

### 🚨 Система алертов
- 4 уровня алертов (INFO, WARNING, ERROR, CRITICAL)
- Автоматические уведомления администраторов в Telegram
- Детектирование массовых ошибок (10+ ошибок/минуту)
- Контекстная информация в алертах

### 🔧 Администрирование
- Панель супер-админа с полным управлением
- Управление товарами и категориями
- Модерация заказов и отзывов
- Настройки анти-спама
- Детальная статистика и аналитика

## Технологический стек

- **Python 3.11+**
- **aiogram 3.x** - фреймворк для Telegram ботов
- **PostgreSQL** - основная база данных
- **asyncpg** - асинхронный драйвер для PostgreSQL
- **SQLAlchemy 2.0** - ORM для работы с БД
- **Redis** - хранилище состояний FSM и кэширование
- **Pydantic** - валидация данных и настроек
- **structlog** - структурированное логирование
- **Alembic** - миграции базы данных
- **Docker & Docker Compose** - контейнеризация

## Структура проекта

```
telegram-shop-bot/
├── src/
│   ├── bot/              # Логика бота
│   │   ├── handlers/     # Обработчики команд и сообщений
│   │   ├── middlewares/  # Промежуточные обработчики
│   │   ├── filters/      # Фильтры для обработчиков
│   │   └── keyboards/    # Клавиатуры (reply и inline)
│   ├── database/         # Работа с базой данных
│   │   ├── models/       # SQLAlchemy модели
│   │   └── repositories/ # Репозитории для работы с моделями
│   ├── services/         # Бизнес-логика
│   ├── core/             # Конфигурация и константы
│   │   ├── config.py     # Настройки приложения
│   │   ├── constants.py  # Константы, enum'ы, сообщения
│   │   └── logging.py    # Настройка логирования
│   ├── utils/            # Вспомогательные утилиты
│   └── main.py           # Точка входа
├── migrations/           # Alembic миграции
├── tests/                # Тесты
├── docker/               # Docker конфигурация
│   ├── Dockerfile
│   └── docker-compose.yml
├── logs/                 # Логи приложения
├── .env                  # Переменные окружения (создать из .env.example)
├── .env.example          # Пример переменных окружения
├── pyproject.toml        # Poetry зависимости и настройки
├── alembic.ini           # Настройки Alembic
└── README.md             # Этот файл
```

## Быстрый старт

### Предварительные требования

- Python 3.11+
- Poetry (для управления зависимостями)
- Docker и Docker Compose (опционально)
- PostgreSQL 14+ (если запускаете без Docker)
- Redis 7+ (если запускаете без Docker)

### Первый запуск с Docker (рекомендуется)

#### Шаг 1: Подготовка проекта

1. Клонируйте репозиторий:
```bash
git clone <repository-url>
cd odezhdabot
```

2. Создайте файл `.env` на основе `.env.example`:
```bash
cp .env.example .env
```

#### Шаг 2: Настройка переменных окружения

Отредактируйте `.env` и установите **обязательные** переменные:

```bash
nano .env  # или любой другой редактор
```

**Критически важные настройки:**

```bash
# Получите токен от @BotFather
BOT_TOKEN=1234567890:ABCdefGHIjklMNOpqrsTUVwxyz

# Ваш Telegram ID (узнайте через @userinfobot)
SUPERADMIN_IDS=123456789,987654321

# ID вашего канала с форумными топиками (получите через @userinfobot)
CHANNEL_ID=-1001234567890

# Пароли (ОБЯЗАТЕЛЬНО измените в production!)
POSTGRES_PASSWORD=your_secure_password_here
REDIS_PASSWORD=your_secure_redis_password
```

**Рекомендуемые настройки для production:**

```bash
# Окружение
ENVIRONMENT=production

# Логирование
LOG_FORMAT=json          # JSON формат для парсинга
LOG_LEVEL=INFO          # INFO для production

# База данных
DB_POOL_SIZE=10         # Размер пула подключений
DB_MAX_OVERFLOW=20      # Максимум дополнительных подключений
```

#### Шаг 3: Запуск сервисов

1. Запустите проект с помощью Docker Compose:
```bash
cd docker
docker-compose up -d
```

2. Дождитесь инициализации (обычно 10-15 секунд):
```bash
docker-compose ps
```

Все сервисы должны быть в статусе `Up (healthy)`.

#### Шаг 4: Применение миграций

Миграции применяются автоматически при запуске, но вы можете применить их вручную:

```bash
# Через Docker
docker-compose exec bot alembic upgrade head

# Или используйте готовый скрипт
./docker/apply-migrations.sh
```

#### Шаг 5: Проверка работы

1. Проверьте логи бота:
```bash
docker-compose logs -f bot
```

Вы должны увидеть:
```
INFO     | Bot started successfully
INFO     | Polling started
```

2. Отправьте `/start` боту в Telegram

3. Проверьте доступ к супер-админ панели:
   - Отправьте `/superadmin`
   - Вы должны увидеть панель управления

#### Шаг 6: Первоначальная настройка

1. **Создайте категории товаров:**
   - `/superadmin` → Товары → Категории
   - Добавьте категории (например: Футболки, Джинсы, Куртки)

2. **Добавьте товары:**
   - `/superadmin` → Товары → Добавить товар
   - Заполните информацию о товаре

3. **Настройте анти-спам:**
   - `/superadmin` → Настройки анти-спама
   - Установите комфортные лимиты

4. **Проверьте статистику:**
   - `/superadmin` → Статистика
   - Убедитесь, что метрики собираются

#### Troubleshooting при первом запуске

**Бот не отвечает:**
```bash
# Проверьте токен
docker-compose logs bot | grep "BOT_TOKEN"

# Проверьте подключение к Telegram
docker-compose logs bot | grep "Polling"
```

**Нет доступа к супер-админ панели:**
```bash
# Проверьте SUPERADMIN_IDS
docker-compose exec bot env | grep SUPERADMIN_IDS

# Убедитесь, что ваш ID правильный (узнайте через @userinfobot)
```

**Ошибка подключения к базе данных:**
```bash
# Проверьте, что PostgreSQL запущен
docker-compose ps postgres

# Проверьте логи PostgreSQL
docker-compose logs postgres

# Проверьте подключение
docker-compose exec postgres psql -U telegram_user -d telegram_bot -c "SELECT 1;"
```

**Миграции не применились:**
```bash
# Примените вручную
docker-compose exec bot alembic upgrade head

# Проверьте текущую версию
docker-compose exec bot alembic current
```

### Установка без Docker

1. Установите Poetry:
```bash
curl -sSL https://install.python-poetry.org | python3 -
```

2. Установите зависимости:
```bash
poetry install
```

3. Создайте и настройте `.env` файл (см. выше)

4. Убедитесь, что PostgreSQL и Redis запущены

5. Создайте базу данных:
```bash
createdb telegram_bot
```

6. Примените миграции:
```bash
poetry run alembic upgrade head
```

7. Запустите бота:
```bash
poetry run python -m src
```

## Управление миграциями

### Автоматическое применение (рекомендуется)

Используйте готовый скрипт:

```bash
./docker/apply-migrations.sh
```

Скрипт автоматически:
- Создает резервную копию БД
- Показывает pending миграции
- Применяет миграции
- Проверяет результат

### Ручное управление

#### Создание новой миграции

```bash
# С Docker
docker-compose exec bot alembic revision --autogenerate -m "описание изменений"

# Без Docker
poetry run alembic revision --autogenerate -m "описание изменений"
```

#### Применение миграций

```bash
# С Docker
docker-compose exec bot alembic upgrade head

# Без Docker
poetry run alembic upgrade head
```

#### Откат миграций

```bash
# Откатить одну миграцию
alembic downgrade -1

# Откатить все миграции
alembic downgrade base

# Откатить до конкретной версии
alembic downgrade <revision_id>
```

#### Проверка статуса миграций

```bash
# Текущая версия
alembic current

# История миграций
alembic history

# Список pending миграций
alembic heads
```

**Подробнее:** см. `migrations/README.md`

## Разработка

### Установка dev-зависимостей

```bash
poetry install --with dev
```

### Запуск тестов

```bash
poetry run pytest
poetry run pytest --cov=src  # с покрытием кода
```

### Форматирование кода

```bash
poetry run black src/
poetry run isort src/
```

### Проверка типов

```bash
poetry run mypy src/
```

### Линтинг

```bash
poetry run flake8 src/
```

## Конфигурация

Все настройки находятся в файле `.env`. Полный список переменных см. в `.env.example`.

### Основные параметры

#### Telegram Bot (обязательные)
- `BOT_TOKEN` - токен бота от @BotFather
- `SUPERADMIN_IDS` - ID супер-администраторов через запятую
- `CHANNEL_ID` - ID канала с форумными топиками (узнайте через @userinfobot)
- `CHANNEL_THREAD_ID` - ID основного топика форума (по умолчанию 1)

#### База данных
- `POSTGRES_HOST` - хост PostgreSQL (по умолчанию localhost)
- `POSTGRES_PORT` - порт PostgreSQL (по умолчанию 5432)
- `POSTGRES_DB` - имя базы данных (по умолчанию telegram_bot)
- `POSTGRES_USER` - пользователь БД (по умолчанию telegram_user)
- `POSTGRES_PASSWORD` - пароль БД (**обязательно измените!**)
- `DB_POOL_SIZE` - размер пула подключений (по умолчанию 10)
- `DB_MAX_OVERFLOW` - макс. дополнительных подключений (по умолчанию 20)

#### Redis
- `REDIS_HOST` - хост Redis (по умолчанию localhost)
- `REDIS_PORT` - порт Redis (по умолчанию 6379)
- `REDIS_PASSWORD` - пароль Redis (**обязательно установите!**)
- `REDIS_DB` - номер базы данных (по умолчанию 0)

#### Логирование и мониторинг
- `LOG_LEVEL` - уровень логирования (DEBUG, INFO, WARNING, ERROR, CRITICAL)
- `LOG_FORMAT` - формат логов:
  - `console` - цветной вывод для разработки
  - `json` - структурированный JSON для production
- `LOG_FILE_PATH` - путь к файлу логов (по умолчанию logs/bot.log)
- `LOG_MAX_BYTES` - макс. размер файла логов (по умолчанию 10MB)
- `LOG_BACKUP_COUNT` - количество файлов для ротации (по умолчанию 5)

#### Окружение
- `ENVIRONMENT` - окружение:
  - `development` - режим разработки
  - `production` - production режим
- `DEBUG` - режим отладки (true/false, **НИКОГДА true в production!**)

#### Рассылки
- `BROADCAST_MESSAGES_PER_SECOND` - лимит сообщений в секунду (по умолчанию 20)
- `BROADCAST_BATCH_SIZE` - размер батча для рассылки (по умолчанию 20)

### Конфигурация для разных окружений

#### Development
```bash
ENVIRONMENT=development
LOG_FORMAT=console
LOG_LEVEL=DEBUG
DEBUG=true
```

#### Production
```bash
ENVIRONMENT=production
LOG_FORMAT=json
LOG_LEVEL=INFO
DEBUG=false
```

**Важно:** См. полный `.env.example` с подробными комментариями

## Docker Compose сервисы

- **bot** - Telegram бот
- **postgres** - PostgreSQL база данных
- **redis** - Redis для FSM и кэширования
- **adminer** - Web-интерфейс для управления БД (доступен на http://localhost:8080, только в dev режиме)

### Запуск с Adminer (для разработки)

```bash
docker-compose --profile dev up -d
```

## Основные команды бота

### Для всех пользователей
- `/start` - начало работы с ботом
- `/help` - справка по командам
- `/menu` - показать главное меню
- `/catalog` - каталог товаров
- `/cart` - корзина
- `/orders` - мои заказы

### Для администраторов
- `/admin` - панель администратора
  - Управление заказами
  - Модерация отзывов
  - Просмотр статистики

### Для супер-администраторов
- `/superadmin` - панель супер-администратора
  - **Товары** - управление товарами и категориями
  - **Рассылка** - создание и управление рассылками
  - **Статистика** - детальная аналитика и метрики
  - **Настройки** - настройки анти-спама и системы

## Архитектура

### Слои приложения

1. **Handlers** - обработка входящих сообщений и команд
2. **Middlewares** - промежуточная обработка (логирование, БД, аутентификация)
3. **Repositories** - работа с базой данных
4. **Services** - бизнес-логика
5. **Models** - модели данных

### Паттерны

- **Repository Pattern** - для работы с БД
- **Dependency Injection** - через middleware
- **FSM** (Finite State Machine) - для управления состояниями диалогов
- **Factory Pattern** - для создания клавиатур

## Производительность

- Использование connection pooling для PostgreSQL
- Redis для хранения состояний FSM
- Асинхронная архитектура (async/await)
- Оптимизированные Docker образы (multi-stage build)

## Безопасность

- Хранение секретов в `.env` файле
- Валидация данных через Pydantic
- Проверка прав доступа через фильтры
- Запуск контейнера от непривилегированного пользователя

## Мониторинг, логирование и алерты

### Логирование

Система использует **structured logging** с библиотекой `structlog`.

**Место хранения логов:**
- Консоль (stdout) - для Docker logs
- Файл `logs/bot.log` - с автоматической ротацией (10MB × 5 файлов)

**Форматы логов:**
- `console` - цветной вывод для разработки
- `json` - структурированный JSON для production и парсинга

**Пример JSON лога:**
```json
{
  "timestamp": "2026-01-22T12:35:14.868009",
  "level": "info",
  "event": "Order created",
  "logger": "src.services.order_service",
  "environment": "production",
  "service": "telegram-bot",
  "user_id": 12345,
  "order_id": 42,
  "processing_time": "0.125s"
}
```

**Автоматическая маскировка чувствительных данных:**
- Телефоны: `+79123456789` → `+7912345**`
- Email: `user@example.com` → `u***@example.com`
- Контакты: логируется только тип, не данные

### Мониторинг метрик

MonitoringService собирает 30+ метрик в реальном времени:

**Метрики пользователей:**
- Всего пользователей
- Активные за 24 часа / 7 дней
- Новые пользователи
- По ролям (user, admin, super_admin)
- Заблокированные

**Метрики заказов:**
- Всего заказов и по статусам
- Новые заказы за период
- Конверсия (completed / total)

**Метрики товаров:**
- Всего товаров (активные / неактивные)
- По категориям
- Без категории

**Метрики рассылок:**
- Всего рассылок
- Доставлено успешно / с ошибками
- Success rate (%)

**Доступ к метрикам:**
- Через бота: `/superadmin` → Статистика
- Программно: `MonitoringService(session).get_system_stats()`

### Система алертов

Автоматические уведомления администраторов в Telegram при критических событиях.

**Уровни алертов:**
- **ℹ️ INFO** - информационные сообщения
- **⚠️ WARNING** - предупреждения (подозрительная активность)
- **❌ ERROR** - ошибки (отправляются всем администраторам)
- **🚨 CRITICAL** - критические ошибки (уведомляют всех super_admin)

**Автоматические алерты:**
- Любая необработанная ошибка → ERROR
- 10+ ошибок за минуту → CRITICAL
- Падение компонентов системы → CRITICAL

**Пример алерта:**
```
🚨 CRITICAL

Database connection lost

Details:
• error: connection timeout
• timestamp: 2026-01-22T12:35:14Z
• component: postgresql
```

### Health Check

Проверка состояния компонентов системы:

```bash
# Через бота
/superadmin → Статистика → Health Check

# Программно
monitoring = MonitoringService(session)
health = await monitoring.get_health_check()
```

**Проверяемые компоненты:**
- ✅ База данных (PostgreSQL)
- ✅ Redis
- ✅ Telegram API

### Анализ логов

**Просмотр логов в реальном времени:**
```bash
# Docker
docker-compose logs -f bot

# Файл
tail -f logs/bot.log
```

**Поиск ошибок:**
```bash
# Последние 20 ошибок
docker-compose logs bot | grep -i "error" | tail -20

# JSON query с jq
cat logs/bot.log | jq 'select(.level == "error")'

# Ошибки за последний час
cat logs/bot.log | jq 'select(.level == "error" and .timestamp > "'$(date -u -d '1 hour ago' +%Y-%m-%dT%H:%M:%S)'")'
```

**Мониторинг метрик через логи:**
```bash
# Время обработки запросов
cat logs/bot.log | jq 'select(.processing_time) | .processing_time'

# Статистика по пользователям
cat logs/bot.log | jq 'select(.user_id) | .user_id' | sort | uniq -c
```

**Подробнее:** см. `docs/MONITORING_AND_ALERTS.md`

## Production Deployment

### Подготовка к production

**1. Чеклист безопасности:**

```bash
# Проверьте .env
✓ Уникальные пароли для БД и Redis
✓ ENVIRONMENT=production
✓ DEBUG=false
✓ LOG_FORMAT=json
✓ SUPERADMIN_IDS заполнены
✓ BOT_TOKEN валиден
✓ CHANNEL_ID правильный
```

**2. Настройка сервера:**

```bash
# Установите Docker и Docker Compose
curl -fsSL https://get.docker.com -o get-docker.sh
sh get-docker.sh

# Клонируйте проект
git clone <repository-url>
cd odezhdabot

# Настройте .env
cp .env.example .env
nano .env
```

**3. Применение миграций:**

```bash
# Создайте резервную копию (если есть данные)
docker-compose exec postgres pg_dump -U telegram_user telegram_bot > backup_$(date +%Y%m%d).sql

# Примените миграции
./docker/apply-migrations.sh
```

**4. Запуск:**

```bash
cd docker
docker-compose up -d

# Проверьте статус
docker-compose ps
docker-compose logs -f bot
```

**5. Настройка автозапуска:**

```bash
# Создайте systemd service
sudo nano /etc/systemd/system/telegram-bot.service
```

```ini
[Unit]
Description=Telegram Shop Bot
After=docker.service
Requires=docker.service

[Service]
Type=oneshot
RemainAfterExit=yes
WorkingDirectory=/path/to/odezhdabot/docker
ExecStart=/usr/bin/docker-compose up -d
ExecStop=/usr/bin/docker-compose down
Restart=on-failure

[Install]
WantedBy=multi-user.target
```

```bash
sudo systemctl enable telegram-bot
sudo systemctl start telegram-bot
```

**6. Настройка мониторинга:**

```bash
# Автоматическая проверка health check
crontab -e
```

```cron
# Проверка каждые 5 минут
*/5 * * * * docker-compose -f /path/to/odezhdabot/docker/docker-compose.yml ps | grep -q "Up" || docker-compose -f /path/to/odezhdabot/docker/docker-compose.yml restart
```

**7. Настройка резервного копирования:**

```bash
# Ежедневный бэкап БД в 3:00
0 3 * * * docker-compose -f /path/to/odezhdabot/docker/docker-compose.yml exec -T postgres pg_dump -U telegram_user telegram_bot | gzip > /backups/telegram_bot_$(date +\%Y\%m\%d).sql.gz
```

**8. Логирование:**

```bash
# Ротация логов Docker
sudo nano /etc/docker/daemon.json
```

```json
{
  "log-driver": "json-file",
  "log-opts": {
    "max-size": "10m",
    "max-file": "3"
  }
}
```

```bash
sudo systemctl restart docker
```

**9. Firewall:**

```bash
# Разрешите только необходимые порты
sudo ufw allow 22/tcp     # SSH
sudo ufw allow 443/tcp    # HTTPS (если используете webhook)
sudo ufw enable
```

**10. Мониторинг:**

- Настройте алерты в Telegram (автоматически через `SUPERADMIN_IDS`)
- Регулярно проверяйте `/superadmin` → Статистика → Health Check
- Мониторьте логи: `docker-compose logs -f bot`

### Обновление production

```bash
# 1. Создайте резервную копию
docker-compose exec postgres pg_dump -U telegram_user telegram_bot > backup_$(date +%Y%m%d).sql

# 2. Остановите бота
docker-compose stop bot

# 3. Обновите код
git pull origin main

# 4. Примените миграции
./docker/apply-migrations.sh

# 5. Пересоберите образ (если изменился код)
docker-compose build bot

# 6. Запустите бота
docker-compose up -d bot

# 7. Проверьте логи
docker-compose logs -f bot
```

### Масштабирование

**Вертикальное:**
```bash
# Увеличьте ресурсы в docker-compose.yml
services:
  bot:
    deploy:
      resources:
        limits:
          cpus: '2'
          memory: 2G
```

**Горизонтальное:**
```bash
# Настройте несколько инстансов с load balancer
# Используйте Redis для shared state
```

**Подробнее:** см. `docs/PRODUCTION_CHECKLIST.md`

## Troubleshooting

### Бот не запускается

1. Проверьте правильность `BOT_TOKEN`
2. Убедитесь, что PostgreSQL и Redis доступны
3. Проверьте логи: `docker-compose logs bot`

### Ошибки подключения к БД

1. Проверьте, что PostgreSQL запущен
2. Проверьте параметры подключения в `.env`
3. Убедитесь, что база данных создана

### Миграции не применяются

1. Проверьте настройки в `alembic.ini`
2. Убедитесь, что база данных доступна
3. Проверьте версию миграций: `alembic current`

## Документация

Полная документация находится в директории `docs/`:

### Быстрый старт
- **[GETTING_STARTED.md](docs/GETTING_STARTED.md)** 🚀 - для новичков:
  - Запуск бота за 5 шагов
  - Минимальная конфигурация
  - Первоначальная настройка
  - Тестирование функций
  - Troubleshooting для первого запуска
  - Полезные команды

### Основная документация
- **[CODE_STYLE.md](docs/CODE_STYLE.md)** - руководство по стилю кода:
  - Naming conventions (PascalCase, snake_case)
  - Type hints и docstrings (Google style)
  - Async/await best practices
  - Error handling patterns
  - Database optimization (N+1 prevention, transactions)
  - Security patterns
  - Performance optimization
  - Structured logging
  - Code review checklist

- **[PRODUCTION_CHECKLIST.md](docs/PRODUCTION_CHECKLIST.md)** - checklist для production:
  - Конфигурация окружения
  - Настройка сервера
  - Security hardening
  - Backup strategy
  - Monitoring setup
  - Troubleshooting guide
  - Performance tuning

- **[MONITORING_AND_ALERTS.md](docs/MONITORING_AND_ALERTS.md)** - система мониторинга:
  - Structured logging с structlog
  - MonitoringService API
  - Alert system (4 уровня)
  - Stats dashboard
  - Health checks
  - Log analysis examples

### Миграции
- **[migrations/README.md](migrations/README.md)** - управление миграциями:
  - Создание миграций
  - Применение и откат
  - Production deployment
  - Troubleshooting

### Системная документация
- **[docs/BROADCAST_SYSTEM.md](docs/BROADCAST_SYSTEM.md)** - система рассылок
- **[docs/FORUM_TOPICS.md](docs/FORUM_TOPICS.md)** - работа с форумными топиками

## Контрибьюция

1. Создайте ветку для фичи: `git checkout -b feature/amazing-feature`
2. Закоммитьте изменения: `git commit -m 'Add amazing feature'`
3. Запушьте ветку: `git push origin feature/amazing-feature`
4. Создайте Pull Request

## Лицензия

MIT License

## Контакты

При возникновении вопросов создавайте issue в репозитории.

---

**Сделано с ❤️ и Python**
