# Управление темами форума

## Обзор

Реализована удобная система привязки категорий товаров к темам форума с возможностью автоматического создания новых тем прямо через бота.

## Возможности

### 1. Автоматическое создание тем

Бот может создавать новые темы в форуме Telegram с выбором цвета иконки:
- 🔵 Синий
- 🟡 Желтый
- 🟣 Фиолетовый
- 🟢 Зеленый
- 🌸 Розовый
- 🔴 Красный

### 2. Ручной ввод thread_id

Для существующих тем можно ввести thread_id вручную.

## Как использовать

### Создание новой темы

1. Откройте **Супер-админ панель**: `/superadmin`
2. Выберите **📁 Управление категориями**
3. Выберите категорию или создайте новую
4. Нажмите **🔗 Привязать к теме**
5. Выберите **🆕 Создать новую тему**
6. Выберите цвет иконки
7. Тема будет автоматически создана и привязана к категории

### Привязка существующей темы

1. Откройте категорию
2. Нажмите **🔗 Привязать к теме**
3. Выберите **🔢 Ввести thread_id вручную**
4. Введите ID темы

#### Как узнать thread_id существующей темы?

**Способ 1: Через @userinfobot**
1. Откройте тему в Telegram
2. Перешлите любое сообщение из темы боту @userinfobot
3. Бот покажет `message_thread_id`

**Способ 2: Из URL темы**
```
https://t.me/c/1234567890/1/123
                          ^^^
                      thread_id
```

**Способ 3: Через API**
Используйте метод `getUpdates` или webhook для получения `message_thread_id` из сообщений.

## Требования

### Настройка канала

1. **Создайте супергруппу** в Telegram
2. **Включите темы**:
   - Настройки группы → Темы → Включить
3. **Добавьте бота** как администратора с правами:
   - Управление темами
   - Отправка сообщений
4. **Получите ID канала** через @userinfobot
5. **Добавьте в `.env`**:
   ```env
   CHANNEL_ID=-1001234567890
   ```

### Настройка .env

```env
# ID канала-форума (супергруппы с темами)
CHANNEL_ID=-1001234567890

# ID должен быть отрицательным числом для супергрупп
# Формат: -100XXXXXXXXXX
```

## Структура данных

### Category модель

```python
class Category(Base):
    id: int
    name: str                    # Название категории
    thread_id: int | None        # ID темы в форуме
    channel_message_id: int | None
    is_active: bool
    created_at: datetime
```

### ForumService методы

```python
# Создать тему
thread_id = await ForumService.create_forum_topic(
    bot=bot,
    chat_id=channel_id,
    name="Категория товаров",
    icon_color="blue",
)

# Редактировать тему
await ForumService.edit_forum_topic(
    bot=bot,
    chat_id=channel_id,
    message_thread_id=thread_id,
    name="Новое название",
)

# Удалить тему
await ForumService.delete_forum_topic(
    bot=bot,
    chat_id=channel_id,
    message_thread_id=thread_id,
)

# Закрыть тему (запретить писать обычным пользователям)
await ForumService.close_forum_topic(
    bot=bot,
    chat_id=channel_id,
    message_thread_id=thread_id,
)

# Открыть тему
await ForumService.reopen_forum_topic(
    bot=bot,
    chat_id=channel_id,
    message_thread_id=thread_id,
)
```

## Публикация товаров

После привязки категории к теме, все товары этой категории будут публиковаться в соответствующую тему:

```python
# При публикации товара
await ProductService.publish_to_channel(
    product_id=123,
    bot=bot,
    channel_id=settings.channel_id,
)

# Товар будет опубликован в тему product.category.thread_id
```

## Пример использования

### Создание категории с темой

```python
# 1. Создать категорию
category = await category_repo.create(name="🧥 Верхняя одежда")

# 2. Создать тему в форуме
thread_id = await ForumService.create_forum_topic(
    bot=bot,
    chat_id=settings.channel_id,
    name="🧥 Верхняя одежда",
    icon_color="blue",
)

# 3. Привязать тему к категории
await category_repo.update(category.id, thread_id=thread_id)

# 4. Добавить товары в категорию
product = await product_service.add_product(
    name="Куртка зимняя",
    category_id=category.id,
    ...
)

# 5. Опубликовать товар
await product_service.publish_to_channel(
    product_id=product.id,
    bot=bot,
    channel_id=settings.channel_id,
)
# Товар опубликуется в тему "🧥 Верхняя одежда"
```

## Обработка ошибок

### Типичные ошибки

**1. "Bad Request: chat not found"**
- Проверьте правильность CHANNEL_ID
- ID должен быть отрицательным: `-1001234567890`

**2. "Bad Request: not enough rights to manage topics"**
- Добавьте бота как администратора
- Дайте право "Управление темами"

**3. "Bad Request: topics are not enabled"**
- Включите темы в настройках группы

**4. "Канал не настроен"**
- Добавьте CHANNEL_ID в .env файл

## Лучшие практики

1. **Используйте автоматическое создание** тем для новых категорий
2. **Выбирайте разные цвета** для визуального разделения категорий
3. **Давайте понятные названия** категориям - они станут названиями тем
4. **Не удаляйте темы** вручную из Telegram - управляйте через бота
5. **Проверяйте привязку** перед публикацией товаров

## Миграция существующих категорий

Если у вас уже есть категории без привязанных тем:

1. Откройте каждую категорию
2. Выберите **🔗 Привязать к теме**
3. Используйте **🆕 Создать новую тему** или **🔢 Ввести thread_id вручную**

## FAQ

**Q: Можно ли изменить название темы?**
A: Да, через `ForumService.edit_forum_topic()` или вручную в Telegram.

**Q: Что произойдет, если удалить тему в Telegram?**
A: thread_id останется в категории, но публикация будет невозможна. Нужно создать новую тему или удалить привязку.

**Q: Можно ли привязать несколько категорий к одной теме?**
A: Технически да, но не рекомендуется для удобства организации.

**Q: Как отвязать категорию от темы?**
A: Пока доступно только через обновление БД напрямую. Скоро добавим UI для этого.

## Технические детали

### Цвета иконок (hex коды)

```python
ICON_COLORS = {
    "blue": 0x6FB9F0,      # 🔵
    "yellow": 0xFFD67E,    # 🟡
    "purple": 0xCB86DB,    # 🟣
    "green": 0x8EEE98,     # 🟢
    "pink": 0xFF93B2,      # 🌸
    "red": 0xFB6F5F,       # 🔴
}
```

### Telegram Bot API методы

- `create_forum_topic` - создать тему
- `edit_forum_topic` - редактировать
- `delete_forum_topic` - удалить
- `close_forum_topic` - закрыть
- `reopen_forum_topic` - открыть
- `get_forum_topic_icon_stickers` - получить стикеры для иконок

## Дополнительные возможности

В будущем планируется:
- [ ] Автоматическая синхронизация названий категорий и тем
- [ ] Отвязка категории от темы через UI
- [ ] Массовое создание тем для всех категорий
- [ ] Закрытие/открытие тем через бота
- [ ] Статистика по темам (количество сообщений, просмотры)
