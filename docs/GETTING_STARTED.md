# Быстрый старт

Это руководство поможет вам запустить бота за 10 минут.

## Что вам понадобится

1. **Docker и Docker Compose** (рекомендуется) или Python 3.11+
2. **Telegram Bot Token** - получите от [@BotFather](https://t.me/BotFather)
3. **Ваш Telegram ID** - узнайте через [@userinfobot](https://t.me/userinfobot)
4. **Telegram канал с форумными топиками** (опционально)

## Запуск за 5 шагов

### Шаг 1: Клонирование проекта

```bash
git clone <repository-url>
cd odezhdabot
```

### Шаг 2: Настройка окружения

Создайте файл `.env`:

```bash
cp .env.example .env
nano .env  # или любой другой редактор
```

**Минимальная конфигурация:**

```bash
# Получите от @BotFather
BOT_TOKEN=1234567890:ABCdefGHIjklMNOpqrsTUVwxyz

# Ваш ID от @userinfobot
SUPERADMIN_IDS=123456789

# ОБЯЗАТЕЛЬНО измените пароли!
POSTGRES_PASSWORD=your_secure_password_here
REDIS_PASSWORD=your_secure_redis_password

# Если есть канал с форумами
CHANNEL_ID=-1001234567890
```

### Шаг 3: Запуск Docker Compose

```bash
cd docker
docker-compose up -d
```

Подождите 10-15 секунд для инициализации.

### Шаг 4: Проверка

```bash
# Проверьте статус сервисов
docker-compose ps

# Проверьте логи
docker-compose logs -f bot
```

Вы должны увидеть:
```
INFO     | Bot started successfully
INFO     | Polling started
```

### Шаг 5: Первый контакт

1. Откройте Telegram и найдите своего бота
2. Отправьте `/start`
3. Отправьте `/superadmin` - вы должны увидеть панель администратора

**Поздравляем! 🎉 Бот запущен и работает!**

## Первоначальная настройка

### 1. Создайте категории товаров

```
/superadmin → Товары → Категории → Добавить категорию
```

Примеры категорий:
- Футболки
- Джинсы
- Куртки
- Обувь

### 2. Добавьте первый товар

```
/superadmin → Товары → Добавить товар
```

Заполните:
- Название товара
- Описание
- Цену
- Категорию
- Фото (опционально)
- Ссылку на форумный топик (опционально)

### 3. Настройте анти-спам

```
/superadmin → Настройки анти-спама
```

Рекомендуемые значения:
- Интервал между сообщениями: 1 секунда
- Максимум сообщений: 10
- Временной интервал: 60 секунд

### 4. Проверьте статистику

```
/superadmin → Статистика → Общая статистика
```

Вы увидите метрики по:
- Пользователям
- Заказам
- Товарам
- Рассылкам

## Тестирование

### Проверка пользовательских функций

1. Откройте бота с другого аккаунта (или попросите друга)
2. Отправьте `/start`
3. Перейдите в каталог товаров
4. Добавьте товар в корзину
5. Оформите заказ

### Проверка админских функций

1. От своего аккаунта откройте `/superadmin`
2. Перейдите в раздел "Товары"
3. Вы должны видеть созданный заказ
4. Измените статус заказа на "В обработке"

### Проверка рассылок

1. `/superadmin` → Рассылка → Новая рассылка
2. Создайте тестовую рассылку с текстом "Привет! Это тестовая рассылка"
3. Отправьте себе
4. Проверьте историю рассылок

## Что дальше?

### Для разработки

- Прочитайте [CODE_STYLE.md](CODE_STYLE.md) - стандарты кодирования
- Изучите [DATABASE_ARCHITECTURE.md](DATABASE_ARCHITECTURE.md) - структура БД
- Посмотрите [NAVIGATION.md](NAVIGATION.md) - система навигации

### Для production

- **ОБЯЗАТЕЛЬНО** прочитайте [PRODUCTION_CHECKLIST.md](PRODUCTION_CHECKLIST.md)
- Настройте мониторинг по [MONITORING_AND_ALERTS.md](MONITORING_AND_ALERTS.md)
- Настройте резервное копирование

### Кастомизация

- Измените тексты в `src/core/constants.py`
- Настройте внешний вид клавиатур в `src/bot/keyboards/`
- Добавьте свою бизнес-логику в `src/services/`

## Troubleshooting

### Бот не отвечает

**Проблема:** Бот не отвечает на `/start`

**Решение:**
```bash
# Проверьте токен
docker-compose logs bot | grep "BOT_TOKEN"

# Проверьте подключение
docker-compose logs bot | grep "Polling"

# Перезапустите бота
docker-compose restart bot
```

### Нет доступа к супер-админ панели

**Проблема:** При `/superadmin` бот говорит "У вас нет прав"

**Решение:**
```bash
# Проверьте SUPERADMIN_IDS
docker-compose exec bot env | grep SUPERADMIN_IDS

# Узнайте свой ID
# Отправьте сообщение @userinfobot и добавьте ID в .env

# Перезапустите бота
docker-compose restart bot
```

### База данных не работает

**Проблема:** Ошибки "database connection"

**Решение:**
```bash
# Проверьте статус PostgreSQL
docker-compose ps postgres

# Проверьте логи
docker-compose logs postgres

# Попробуйте подключиться вручную
docker-compose exec postgres psql -U telegram_user -d telegram_bot -c "SELECT 1;"
```

### Миграции не применились

**Проблема:** Ошибки про отсутствующие таблицы/колонки

**Решение:**
```bash
# Примените миграции
docker-compose exec bot alembic upgrade head

# Или используйте скрипт
./docker/apply-migrations.sh
```

## Полезные команды

### Docker Compose

```bash
# Запустить все сервисы
docker-compose up -d

# Остановить все сервисы
docker-compose down

# Перезапустить бота
docker-compose restart bot

# Посмотреть логи
docker-compose logs -f bot

# Посмотреть статус
docker-compose ps

# Зайти в контейнер
docker-compose exec bot bash
```

### Работа с базой данных

```bash
# Подключиться к PostgreSQL
docker-compose exec postgres psql -U telegram_user -d telegram_bot

# Создать бэкап
docker-compose exec postgres pg_dump -U telegram_user telegram_bot > backup.sql

# Восстановить из бэкапа
cat backup.sql | docker-compose exec -T postgres psql -U telegram_user -d telegram_bot
```

### Миграции

```bash
# Посмотреть текущую версию
docker-compose exec bot alembic current

# Применить миграции
docker-compose exec bot alembic upgrade head

# Откатить миграцию
docker-compose exec bot alembic downgrade -1
```

## Получение помощи

Если у вас возникли проблемы:

1. **Проверьте логи:**
   ```bash
   docker-compose logs -f bot
   ```

2. **Проверьте документацию:**
   - [README.md](../README.md) - основная документация
   - [PRODUCTION_CHECKLIST.md](PRODUCTION_CHECKLIST.md) - production setup
   - [MONITORING_AND_ALERTS.md](MONITORING_AND_ALERTS.md) - мониторинг

3. **Создайте issue** в репозитории с:
   - Описанием проблемы
   - Логами из `docker-compose logs bot`
   - Версией Docker и Docker Compose
   - Содержимым `.env` (БЕЗ токенов и паролей!)

## Следующие шаги

После успешного запуска:

- ✅ Настройте production окружение
- ✅ Настройте резервное копирование
- ✅ Настройте мониторинг
- ✅ Протестируйте все функции
- ✅ Добавьте свои товары
- ✅ Пригласите тестовых пользователей

**Удачи с вашим ботом! 🚀**
