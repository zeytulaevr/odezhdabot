# Инструкция по развертыванию на сервере

## 1. Подготовка сервера

Убедитесь, что на сервере установлены:
- Docker (версия 20.10+)
- Docker Compose (версия 2.0+)
- Git

```bash
# Проверка версий
docker --version
docker compose version
git --version
```

## 2. Клонирование репозитория

```bash
cd /opt  # или любая другая директория
git clone <your-repo-url> odezhdabot
cd odezhdabot
```

## 3. Настройка переменных окружения

Скопируйте пример конфигурации и отредактируйте:

```bash
cp .env.example .env
nano .env  # или vim .env
```

**ВАЖНО! Обязательно измените следующие параметры:**

```env
# Токен вашего бота от @BotFather
BOT_TOKEN=ваш_настоящий_токен

# Окружение
ENVIRONMENT=production

# База данных (используйте надежные пароли!)
POSTGRES_DB=telegram_bot
POSTGRES_USER=botuser
POSTGRES_PASSWORD=ваш_надежный_пароль_123!@#

# Redis (используйте надежный пароль!)
REDIS_PASSWORD=ваш_надежный_пароль_456!@#

# ID супер-администратора (ваш Telegram ID)
# Узнать можно у @userinfobot
SUPERADMIN_IDS=ваш_telegram_id

# ID канала (если используете)
CHANNEL_ID=-100xxxxxxxxxx

# Логирование для production
LOG_LEVEL=INFO
LOG_FORMAT=json
DEBUG=false
```

## 4. Запуск бота

### Первый запуск

```bash
cd docker
docker compose up -d
```

### Проверка статуса

```bash
docker compose ps
docker compose logs -f bot  # Просмотр логов бота
```

### Остановка

```bash
docker compose down
```

### Перезапуск после изменений

```bash
docker compose down
docker compose up -d --build
```

## 5. Обновление кода

```bash
# Остановить бота
cd /opt/odezhdabot/docker
docker compose down

# Обновить код
cd /opt/odezhdabot
git pull origin main

# Запустить снова
cd docker
docker compose up -d --build
```

## 6. Миграции базы данных

После обновления кода выполните миграции:

```bash
# Войти в контейнер
docker exec -it telegram-bot bash

# Выполнить миграции
alembic upgrade head

# Выйти из контейнера
exit
```

## 7. Резервное копирование

### Бэкап базы данных

```bash
# Создать бэкап
docker exec telegram-bot-postgres pg_dump -U botuser telegram_bot > backup_$(date +%Y%m%d_%H%M%S).sql

# Восстановить из бэкапа
docker exec -i telegram-bot-postgres psql -U botuser telegram_bot < backup_20260127_120000.sql
```

### Бэкап volumes

```bash
# Создать архив с данными
docker run --rm \
  -v telegram-bot_postgres_data:/data \
  -v $(pwd):/backup \
  alpine tar czf /backup/postgres_backup_$(date +%Y%m%d).tar.gz -C /data .
```

## 8. Мониторинг и логи

### Просмотр логов

```bash
# Все логи
docker compose logs -f

# Только бот
docker compose logs -f bot

# Только PostgreSQL
docker compose logs -f postgres

# Последние 100 строк
docker compose logs --tail=100 bot
```

### Просмотр использования ресурсов

```bash
docker stats
```

## 9. Решение проблем

### Бот не запускается

```bash
# Проверить логи
docker compose logs bot

# Проверить переменные окружения
docker compose config

# Пересоздать контейнеры
docker compose down -v
docker compose up -d
```

### Проблемы с базой данных

```bash
# Проверить healthcheck
docker compose ps

# Войти в PostgreSQL
docker exec -it telegram-bot-postgres psql -U botuser -d telegram_bot

# Проверить подключение
\conninfo
\dt  # Список таблиц
\q   # Выход
```

### Порты заняты

Если на сервере уже работает PostgreSQL/Redis на стандартных портах, убедитесь что в `docker-compose.yml` порты закомментированы (как в текущей конфигурации). Бот работает внутри Docker-сети и не требует проброса портов наружу.

## 10. Безопасность

1. **Используйте сильные пароли** для POSTGRES_PASSWORD и REDIS_PASSWORD
2. **Не пробрасывайте порты** БД и Redis наружу (текущая конфигурация правильная)
3. **Используйте firewall** для защиты сервера
4. **Регулярно обновляйте** Docker-образы:
   ```bash
   docker compose pull
   docker compose up -d
   ```
5. **Делайте резервные копии** базы данных

## 11. Автоматический перезапуск

Контейнеры настроены на `restart: unless-stopped`, поэтому будут автоматически перезапускаться:
- После перезагрузки сервера
- После сбоя

## 12. Проверка работы админ-панели

После запуска бота:

1. Откройте бота в Telegram
2. Отправьте `/start`
3. Если ваш ID в `SUPERADMIN_IDS`, вы увидите кнопку **"👑 Супер-админ панель"** внизу экрана
4. Нажмите на кнопку для открытия панели управления

## Контакты

При возникновении проблем проверьте логи и обратитесь к документации в репозитории.
